export const launchActivationProductionFlagsEnvironment = "production";
// A candidate redeploy raises this code-pinned floor to permanently revoke
// every still-valid activation envelope from an older generation.
export const launchActivationMinimumGeneration = 2;
